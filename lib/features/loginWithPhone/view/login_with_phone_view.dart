import 'package:flutter/material.dart';
import 'package:photographer/features/loginWithPhone/view/widget/login_with_phone_body.dart';
class LoginWithPhoneView extends StatelessWidget {
  const LoginWithPhoneView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: LoginWithPhoneBody());
  }
}
