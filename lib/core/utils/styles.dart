import 'package:flutter/material.dart';

import 'constants.dart';

abstract class Styles {
  static const hintTextField = TextStyle(
    fontSize: 18,
    color: Color(0xFF101010),
  );
  static const titleLogin=TextStyle(
    color: kColorAppBar,
    fontSize: 40,
    fontWeight: FontWeight.bold
  );
}
